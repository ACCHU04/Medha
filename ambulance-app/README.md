# Ambulance App

Future-facing directory for the ambulance client application.

**Not implemented yet.** Phase-1 UI lives in `backend/app/static/ambulance-simulator/`. This directory will hold the real ambulance app (mobile/web) in a later phase.
